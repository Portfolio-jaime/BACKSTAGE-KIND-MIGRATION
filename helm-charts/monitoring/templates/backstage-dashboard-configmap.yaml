apiVersion: v1
kind: ConfigMap
metadata:
  name: backstage-grafana-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  backstage-dashboard.json: |
{{ .Files.Get "templates/backstage-dashboard.json" | indent 4 }}
